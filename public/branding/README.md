Place ANVA branding assets here:
- anva-logo.svg (used in document header)
- anva-stamp.svg (used in document signature/footer area)

These filenames are referenced by the shared template renderer in src/lib/documentTemplate.ts.
